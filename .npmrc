auto-install-peers=false
strict-peer-dependencies=false

# React Native / Expo Android builds require a flat node_modules layout
# so that Gradle can resolve the react-native package path. pnpm's default
# isolated layout causes the prebuild to emit a null path for react-native.
node-linker=hoisted
